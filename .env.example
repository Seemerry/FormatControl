# 语义修复模型配置（用于处理语法层面无法判断的歧义片段）
# 支持任何 OpenAI 兼容的 API
REPAIR_API_URL=https://api.deepseek.com
REPAIR_API_KEY=sk-your-api-key-here
REPAIR_MODEL_ID=deepseek-chat

# 设为 false 可全局禁用语义修复，仅使用语法修复
REPAIR_SEMANTIC_ENABLED=true
